import angular from 'angular'
import component from './gameSettingsComponent'
import './gameSettings.css'

export default angular
  .module('gameSettings', [
  ])
  .component('gameSettings', component)
  .name
